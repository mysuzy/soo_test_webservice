pluginManagement {
    plugins {
    }
    resolutionStrategy {
    }
    repositories {
        maven { url "https://plugins.gradle.org/m2/" }
        gradlePluginPortal()
    }
}

rootProject.name = 'test_webservice'

